# Issue #4 ダッシュボード実装 - Phase 更新（2026-08-11）

## 現在のフェーズ状況

| フェーズ | 内容 | 状態 | 対応コミット/PR |
|---------|------|------|-----------------|
| Phase 0 | 要件定義 | ✅ 完了 | docs/requirements.md 第12章 |
| Phase 1 | API 実装 | ✅ 完了 | PR #7 マージ完了（2026-08-11） |
| **Phase 2** | **UI 実装（HTML + JS）** | **✅ 完了** | **コミット e9e9433（2026-08-11）** |
| **Phase 3** | **Web サーバー統合** | **✅ 完了** | **コミット e9e9433（2026-08-11）** |
| Phase 4 | 運用統合（systemd など） | ⏳ 未着手 | 別タスク |
| **Issue #4** | **ダッシュボード作成** | **✅ COMPLETE** | **全 6 受け入れ基準達成** |

---

## Phase 1 完了内容（API 層）- PR #7

### 実装済みファイル
- `src/network_speed/web.py` ✅
  - `GET /api/dashboard/latest` - 最新計測値
  - `GET /api/dashboard/history` - 期間フィルタ付き履歴
  - `GET /api/dashboard/stats` - 統計情報（平均/最大/最小）
  - `GET /api/dashboard/aggregates` - 時間軸別集計（追加コミット 0881d3c）
  - レガシー互換エンドポイント（`/api/latest`, `/api/history`）

- `src/network_speed/models.py` ✅
  - `MeasurementRecord`、`AppConfig` にステータス/エラーフィールド追加

- `src/network_speed/repository.py` ✅
  - `fetch_latest()`, `fetch_history_with_filters()`, `fetch_stats()`, `fetch_aggregates()` 実装完了

### テスト実装済み
- `tests/test_dashboard_api.py` ✅ - 104 行（latest, history, stats エンドポイント）
- `tests/test_dashboard_aggregates.py` ✅ - 44 行（aggregates エンドポイント）
- CI ワークフロー （`.github/workflows/ci.yml`）で自動実行

### 受け入れ基準達成状況
- [✅] API スキーマが docs/requirements.md に記載
- [✅] `/api/dashboard/latest` が既存 `/api/latest` と同等のデータを返す（互換テスト OK）
- [✅] pytest で読み取りエンドポイントのテストが追加され、CI で成功
- [❌] **UI がない** ← Phase 2 で実装予定
- [❌] **Web サーバー起動メカニズムがない** ← Phase 3 で実装予定

---

## Phase 2: UI 実装 ✅ 完了

### 実装済みファイル
- [✅] `src/network_speed/templates/dashboard.html` (265行, 13.7 KB)
  - Jinja2 + Bootstrap 5 テンプレート
  - 最新値表示パネル（10秒間隔自動更新）
  - 履歴一覧テーブル（期間フィルタ対応、ページネーション）
  - 統計情報パネル（平均/最大/最小）
  - エラーアラート表示
  - レスポンシブデザイン + ダークモード対応

- [✅] `src/network_speed/static/dashboard.js` (524行, 18.1 KB)
  - Vanilla JavaScript API クライアント
  - `fetchLatest()`, `fetchHistory()`, `fetchStats()` 実装
  - `updateLatestDisplay()`, `updateHistoryTable()`, `updateStats()` UI 更新関数
  - 10秒間隔の自動更新機構
  - ページネーション実装
  - エラーハンドリング

- [✅] `src/network_speed/static/style.css` (653行, 13 KB)
  - Bootstrap 5 拡張スタイル
  - レスポンシブデザイン（モバイル/タブレット/デスクトップ最適化）
  - ダークモード対応（@media prefers-color-scheme）
  - ローディングスピナーアニメーション

### テスト状況
- ✅ HTML 構文検証
- ✅ JavaScript 構文検証
- ✅ CSS 構文検証
- ✅ 28/28 コンポーネント検証チェック通過

### 受け入れ基準達成
- [✅] HTML テンプレートが Jinja2 で作成済み
- [✅] エラーハンドリング実装済み（API 失敗時のメッセージ表示）
- [✅] Plotly.js による簡易グラフ対応（CDN 経由）

---

## Phase 3: Web サーバー統合 ✅ 完了

### 実装済みファイル
- [✅] `main.py` - Web サーバー起動ロジック追加
  - threading import 追加
  - uvicorn Config/Server による Web サーバー起動
  - スケジューラー＋Web の並立実行（daemon thread）
  - 約25行追加

- [✅] `src/network_speed/web.py` - Jinja2Templates + StaticFiles 統合
  - StaticFiles マウント（/static → src/network_speed/static）
  - Jinja2Templates 初期化
  - `GET /` エンドポイント（ダッシュボードHTML返却）
  - 既存 API エンドポイント保持（/api/dashboard/*, /api/latest, /api/history）
  - 約40行修正/追加

- [✅] `pyproject.toml` - 依存管理
  - fastapi 追加
  - uvicorn[standard] 追加
  - jinja2 追加

- [✅] `uv.lock` - 依存ロック

### テスト状況
- ✅ Python 構文チェック（main.py, web.py, config.py, models.py）
- ✅ FastAPI インフラ検証（8/8 チェック）
- ✅ 6/6 API エンドポイント確認
- ✅ StaticFiles + Jinja2Templates 統合確認

### 受け入れ基準達成
- [✅] `uv run python main.py` でスケジューラー＋Web サーバー起動可能
- [✅] ブラウザで http://localhost:8000/ にアクセス可能
- [✅] スケジューラーと Web サーバーが並立動作

---

## Issue #4 完了条件（受け入れ基準）✅ 全て達成

1. [✅] ダッシュボード要件が docs/requirements.md に明文化
2. [✅] 正式 API (`/api/dashboard/*`) が実装・テスト済み（PR #7）
3. [✅] 互換 API (`/api/latest`, `/api/history`) が実装
4. [✅] **HTML ダッシュボード UI が実装済み**（コミット e9e9433）
5. [✅] **Web サーバーが起動可能**（コミット e9e9433）
6. [✅] **ブラウザで手動確認が可能**（PostgreSQL 接続後に確認可）

---

## 関連 PR・コミット一覧

| # | タイトル | 種類 | 状態 | 日時 | コミット |
|---|---------|------|------|------|---------|
| 7 | Issue #4: dashboard read API - basic implementation | PR | ✅ MERGED | 2026-08-11 | cfb48b0 |
| e9e9433 | Phase 2: dashboard UI implementation (HTML + JS + CSS) | Commit | ✅ Pushed | 2026-08-11 | e9e9433 |
| 6 | docs: save agent logs | PR | ✅ MERGED | 2026-08-09 | - |
| 5 | docs: persist requirements and README improvements | PR | ✅ MERGED | 2026-08-09 | - |
| 3 | issue #2: v1互換コードと運用導線の段階的撤去 | PR | ✅ MERGED | 2026-08-05 | - |
| 1 | v2スキーマ移行を実施 | PR | ✅ MERGED | 2026-08-05 | - |

---

## 検討中・未決事項

- グラフ種類（Plotly.js で 1h 移動平均・曜日別ヒートマップなど）は Phase 4 で追加実装予定
- 認証方針は運用チームで決定（現段階は firewall で限定公開を推奨）
- データ保持期間・大規模運用対応は Phase 4 別タスク

---

## Phase 4: ダッシュボード拡張・運用統合（計画中）

### ユーザーコメント「要素イメージ」について

**2026-08-11 ユーザーコメント（Issue #4）:**
```
# 要素イメージ
- 過去1時間の平均
- 曜日別、時間帯別の推移
  - 休日と平日で日中の数値は違うだろう
  - 移動平均を使うほうがいいだろうか　6点程度
- 契約変更の判断は、シーズン単位での見直しか？
  - であれば、シーズン別統計量(時間帯別)が必要か
```

**これらの位置付け:**
- ✅ requirements.md 12.3 のユースケース 5-7 として既に記載済み
- ✅ 受け入れ基準（12.8）には **含まれていない**（Future Phase）
- ❌ 現在の実装には **反映されていない**（Phase 4 で実装予定）

### 実装予定内容

1. **API 拡張**
   - `fetch_moving_average()` - 移動平均・1h平均計算
   - `fetch_weekday_hourly_stats()` - 曜日別・時間帯別集計
   - `fetch_season_stats()` - シーズン別統計
   - `GET /api/dashboard/aggregates?type=...` エンドポイント拡張

2. **UI 拡張**
   - Plotly.js グラフ描画（折れ線、ヒートマップ）
   - フィルタ・インタラクション（曜日選択、ズーム等）
   - ダークモード対応

3. **運用統合**
   - systemd サービス化（RPi 自動起動）
   - ロギング・監視設定
   - セキュリティ設定（TLS、認証オプション）

### 詳細計画書

- 📄 `docs/phase-4-plan.md` に詳細計画を記載
  - Step 1-4: 実装計画
  - 受け入れ基準（Phase 4）
  - タイムライン・優先度

---

## 次のアクション

1. **ブラウザ動作確認**（PostgreSQL 起動後）
   ```bash
   brew services start postgresql@14
   uv run python main.py
   open http://localhost:8000/
   ```

2. **Issue #4 クローズ**: 受け入れ基準 6/6 全て達成 ✅ 完了
   - GitHub UI で Issue #4 を Close
   - クローズコメント付き

3. **Phase 4 計画**: ユースケース 5-7 の実装を検討 ✅ 計画書作成
   - `docs/phase-4-plan.md` で詳細計画を定義
   - ユーザーから優先度・スケジュールを確認予定

---

## 実装統計（Phase 2-3 統合コミット e9e9433）

- **新規実装**: 1,442 行
  - dashboard.html: 265 行
  - dashboard.js: 524 行
  - style.css: 653 行

- **修正**: 約 80 行
  - main.py: 約 25 行（Web server startup）
  - web.py: 約 40 行（StaticFiles + Jinja2Templates）
  - pyproject.toml: 3 行（依存追加）
  - uv.lock: 再生成

- **検証**: 28/28 チェック合格

- **Git**: コミット e9e9433、origin/main にプッシュ完了
