# Issue #4 ダッシュボード実装 - Phase 更新（2026-08-11）

## 現在のフェーズ状況

| フェーズ | 内容 | 状態 | 対応コミット/PR |
|---------|------|------|-----------------|
| Phase 0 | 要件定義 | ✅ 完了 | docs/requirements.md 第12章 |
| **Phase 1** | **API 実装** | **✅ 完了** | **PR #7 マージ完了（2026-08-11）** |
| **Phase 2** | **UI 実装（HTML + JS）** | **⏳ 次フェーズ** | **未着手** |
| **Phase 3** | **Web サーバー統合** | **⏳ 未着手** | **main.py に Uvicorn 起動ロジック追加が必要** |
| Phase 4 | 運用統合（systemd など） | ⏳ 未着手 | 別タスク |

---

## Phase 1 完了内容（API 層）- PR #7

### 実装済みファイル
- `src/network_speed/web.py` ✅
  - `GET /api/dashboard/latest` - 最新計測値
  - `GET /api/dashboard/history` - 期間フィルタ付き履歴
  - `GET /api/dashboard/stats` - 統計情報（平均/最大/最小）
  - `GET /api/dashboard/aggregates` - 時間軸別集計（追加コミット 0881d3c）
  - レガシー互換エンドポイント（`/api/latest`, `/api/history`）

- `src/network_speed/models.py` ✅
  - `MeasurementRecord`、`AppConfig` にステータス/エラーフィールド追加

- `src/network_speed/repository.py` ✅
  - `fetch_latest()`, `fetch_history_with_filters()`, `fetch_stats()`, `fetch_aggregates()` 実装完了

### テスト実装済み
- `tests/test_dashboard_api.py` ✅ - 104 行（latest, history, stats エンドポイント）
- `tests/test_dashboard_aggregates.py` ✅ - 44 行（aggregates エンドポイント）
- CI ワークフロー （`.github/workflows/ci.yml`）で自動実行

### 受け入れ基準達成状況
- [✅] API スキーマが docs/requirements.md に記載
- [✅] `/api/dashboard/latest` が既存 `/api/latest` と同等のデータを返す（互換テスト OK）
- [✅] pytest で読み取りエンドポイントのテストが追加され、CI で成功
- [❌] **UI がない** ← Phase 2 で実装予定
- [❌] **Web サーバー起動メカニズムがない** ← Phase 3 で実装予定

---

## Phase 2: UI 実装（次フェーズ）

### 実装対象
- [ ] `src/network_speed/templates/dashboard.html` - Jinja2 テンプレート
  - 最新値表示パネル
  - 履歴一覧テーブル（期間フィルタ対応）
  - 統計情報パネル（平均/最大/最小）
  - 簡易グラフ表示（Plotly.js）

- [ ] `src/network_speed/static/dashboard.js` - API 呼び出し + UI 更新
  - `/api/dashboard/latest` リアルタイム更新（毎 10 秒）
  - `/api/dashboard/history` 非同期フェッチ + ページネーション
  - `/api/dashboard/stats` 統計表示

- [ ] `src/network_speed/static/style.css` - Bootstrap + カスタムスタイル

- [ ] `pyproject.toml` に `fastapi`, `uvicorn`, `jinja2` 依存追加

### 受け入れ基準
- [ ] HTML テンプレートが Jinja2 で作成済みこと
- [ ] ブラウザで最新値・履歴取得が動作すること（手動確認）
- [ ] エラーハンドリング実装済み（API 失敗時のメッセージ表示）

---

## Phase 3: Web サーバー統合（その次）

### 実装対象
- [ ] `main.py` に Uvicorn 起動ロジック追加
  - Web サーバーとスケジューラーを同一プロセスで実行
  - `config.web_host`, `config.web_port` で制御
  - スケジューラー・Web 両層のエラーハンドリング統一

- [ ] `src/network_speed/config.py` にこれらの設定を追加
  - `web_host` (デフォルト: `0.0.0.0`)
  - `web_port` (デフォルト: `8000`)
  - `web_static_dir` (デフォルト: `src/network_speed/static`)
  - `web_template_dir` (デフォルト: `src/network_speed/templates`)

- [ ] `pyproject.toml` で FastAPI/Uvicorn を正式依存に追加

### 受け入れ基準
- [ ] `uv run python main.py` 実行でスケジューラー + Web サーバーが起動すること
- [ ] ブラウザで `http://localhost:8000/` にアクセスできること
- [ ] スケジューラーのジョブと Web サーバーが並立動作すること

---

## Issue #4 完了条件（受け入れ基準）

1. [✅] ダッシュボード要件が docs/requirements.md に明文化
2. [✅] 正式 API (`/api/dashboard/*`) が実装・テスト済み（PR #7）
3. [✅] 互換 API (`/api/latest`, `/api/history`) が実装
4. [⏳] **HTML ダッシュボード UI が実装済み** ← Phase 2
5. [⏳] **Web サーバーが起動可能** ← Phase 3
6. [⏳] **ブラウザで手動確認が OK** ← Phase 2-3 完了時

---

## 関連 PR 一覧

| PR # | タイトル | 状態 | マージ日 |
|------|---------|------|---------|
| 7 | Issue #4: dashboard read API - basic implementation | ✅ MERGED | 2026-08-11 |
| 6 | docs: save agent logs | ✅ MERGED | 2026-08-09 |
| 5 | docs: persist requirements and README improvements | ✅ MERGED | 2026-08-09 |
| 3 | issue #2: v1互換コードと運用導線の段階的撤去 | ✅ MERGED | 2026-08-05 |
| 1 | v2スキーマ移行を実施 | ✅ MERGED | 2026-08-05 |

---

## 検討中・未決事項

- グラフ種類（Plotly.js で 1h 移動平均・曜日別ヒートマップなど）は Phase 2 で実装
- 認証方針は運用チームで決定（現段階は firewall で限定公開を推奨）
- データ保持期間・大規模運用対応は別タスク

---

## 次のアクション

1. **Phase 2 開始**: UI 実装（HTML + JS）に着手
2. **Phase 3**: Web サーバー統合（main.py + Uvicorn）
3. **手動テスト**: ブラウザで動作確認
4. **Issue #4 クローズ**: 受け入れ基準 6 点すべて達成時
