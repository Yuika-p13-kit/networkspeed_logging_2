# issue #2: v1互換コードと運用導線の段階的撤去

## 目的の再定義

本番は `v2_only` 前提で稼働しているため、このタスクの目的は、v1 互換コードと旧運用導線を一度に消すことではなく、切り戻し余地を壊さない範囲で依存を段階的に整理し、運用・実装・ドキュメントの整合を `v2_only` 主軸へ寄せること。

## 受け入れ条件

1. 本番運用ドキュメントと監視手順が `v2_only` 前提で矛盾なく読める。
2. 監視コマンドの判定基準が一意で、移行可否を 1 回の実行で判断できる。
3. v1 依存を残す理由と撤去期限が明文化され、保持範囲が説明できる。
4. 変更後も既存テストが通過し、運用互換を壊していないことを確認できる。

## 非スコープ

- `network_speed_measurements` の即時 DROP。
- 障害時切り戻しポリシーの即時廃止。
- DB スキーマの変更。

## 実施順

1. 影響範囲を棚卸しし、v1 互換と旧導線の残存箇所を確定する。
2. README / runbook / 監視説明を先に整理し、`v2_only` 前提へ統一する。
3. `repository.py`、`config.py`、`models.py` の分岐を段階的に削減する。
4. 関連テストを更新し、互換整理後の期待値を固定する。
5. リリース判定として、`v2_only` 前提の監視結果とドキュメントの整合を最終確認する。

## ブランチ / PR の注意点

- 1 つの PR で「ドキュメント整理」と「コード分岐削減」を混在させる場合でも、変更理由は `v2_only` への寄せ方として一貫させる。
- v1 互換の削除は、切り戻し手順の保持を前提に、保持理由が消えるまで急がない。
- 監視コマンドの判定結果が曖昧になる変更は先に入れず、判定条件を明文化してから進める。
- 旧導線の削除前に、README / runbook / テストの記述差分を揃える。

## ダッシュボード要件定義フェーズ開始

- 背景: `v2_only` 運用を前提に、保存済み計測結果の可視化要求が明確化したため、表示/API の要件を先に固定する。
- 目的: 復旧フロー優先方針と矛盾しないダッシュボード要件初版を作成し、後続実装の判断基準を統一する。
- 完了条件: `docs/requirements.md` の追記内容から、運用モード適用範囲（章 3〜11 と章 12）、`status`/`error_summary` の生成規則、正式 API と互換 API の区別、検証可能な受け入れ基準をレビューで一意に確認できること。
- 完了条件: `task.md` に未決事項のユーザー確認ゲートが追加され、確認項目・確認タイミング・合格条件を読んだ人が同じ判断で運用できること。