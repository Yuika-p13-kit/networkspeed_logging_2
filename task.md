# issue #2 実施タスク

## 1. 影響範囲の棚卸し

- v1 互換コード、dual_write 前提の分岐、旧運用導線の残存箇所を列挙する。
- 変更対象を `repository.py`、`config.py`、`models.py`、README、runbook、関連テストに絞る。

## 2. ドキュメント先行整理

- README と `docs/v2-migration-runbook.md` の説明を `v2_only` 主軸に揃える。
- v1 依存を残す理由、切り戻し期間、撤去の前提を短く明記する。
- 監視コマンドの判定条件を一意に読める形へ整理する。

## 3. 設定・実装の段階的削減

- `NETWORK_SPEED_SCHEMA_MIGRATION_MODE` 周辺の分岐を見直す。
- v1 / dual_write 専用の扱いを縮小し、`v2_only` を標準経路として明確化する。
- 互換維持が必要な箇所は、撤去条件が満たされるまで残す。

## 4. テスト更新

- ドキュメント変更に対応する期待値を更新する。
- `v2_only` 前提の設定・監視・運用確認テストを優先して整える。
- 既存テストが通ることを確認する。

## 5. リリース判定

- 監視コマンドの結果とドキュメントの説明が一致していることを確認する。
- v1 依存の保持理由と撤去期限が PR から読み取れることを確認する。
- そのうえでマージ可否を判断する。

## ダッシュボード要件定義フェーズ

- `docs/requirements.md` にダッシュボード要件初版セクションを追記し、既存全体要件との重複を最小化する。
- 対象ユーザーと主要ユースケース（最新確認、期間比較、異常把握）を明文化する。
- 機能要件を整理する（一覧表示、期間フィルタ、最新値、簡易統計、エラーステータス表示）。
- 読み取り専用 API の想定エンドポイントとレスポンス項目を定義する。
- 非機能要件（可用性、応答性能目標、運用性、セキュリティ最低限）を定義する。
- 制約条件（`v2_only` 前提、復旧フロー優先、既存テーブル利用、既存仕様維持）を明記する。
- 受け入れ基準と未決事項（UI 詳細、認証要否、保存期間など）を分離し、次フェーズの確認論点を固定する。

## 未決事項ユーザー確認ゲート

- 実装着手前に、`docs/requirements.md` の未決事項（UI 詳細、認証要否、保存期間・最大取得件数、status/error_summary 最終マッピング）をユーザー確認にかける。
- 確認タイミングは「要件定義フェーズ完了後、実装タスク開始前」とする。
- 合格条件は、未決事項ごとに「採用方針」「暫定運用」「次回見直し時期」の 3 点が記録されていること。
- 未回答項目がある場合は、該当機能の実装タスクを着手不可として保留にする。