# issue #2 実施タスク

## 1. 影響範囲の棚卸し

- v1 互換コード、dual_write 前提の分岐、旧運用導線の残存箇所を列挙する。
- 変更対象を `repository.py`、`config.py`、`models.py`、README、runbook、関連テストに絞る。

## 2. ドキュメント先行整理

- README と `docs/v2-migration-runbook.md` の説明を `v2_only` 主軸に揃える。
- v1 依存を残す理由、切り戻し期間、撤去の前提を短く明記する。
- 監視コマンドの判定条件を一意に読める形へ整理する。

## 3. 設定・実装の段階的削減

- `NETWORK_SPEED_SCHEMA_MIGRATION_MODE` 周辺の分岐を見直す。
- v1 / dual_write 専用の扱いを縮小し、`v2_only` を標準経路として明確化する。
- 互換維持が必要な箇所は、撤去条件が満たされるまで残す。

## 4. テスト更新

- ドキュメント変更に対応する期待値を更新する。
- `v2_only` 前提の設定・監視・運用確認テストを優先して整える。
- 既存テストが通ることを確認する。

## 5. リリース判定

- 監視コマンドの結果とドキュメントの説明が一致していることを確認する。
- v1 依存の保持理由と撤去期限が PR から読み取れることを確認する。
- そのうえでマージ可否を判断する。